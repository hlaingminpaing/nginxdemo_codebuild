version: 0.2

  phases:
    pre_build:
      commands:
      - echo Logging in to Amazon ECR...
      - echo $AWS_DEFAULT_REGION
      - echo $AWS_ACCOUNT_ID
      - echo $IMAGE_REPO_NAME
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      
    build:
      commands:
      - echo Building the Docker image...
      - docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG .
      - docker tag $IMAGE_REPO_NAME:$IMAGE_TAG $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
  
    post_build:
      commands:
      - echo Pushing the Docker image...
      - docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG

# version: 0.2

# phases:
#   pre_build:
#     commands:
#       # Print environment variables for debugging
#       - echo Logging in to Amazon ECR...
#       - echo $AWS_DEFAULT_REGION
#       - echo $AWS_ACCOUNT_ID
#       - echo $IMAGE_REPO_NAME
      
#       # AWS CLI version 2 authentication for ECR
#       # - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 123456789012.dkr.ecr.us-east-1.amazonaws.com
#       - aws ecr get-login-password --region ap-southeast-1 | docker login --username AWS --password-stdin 965080479351.dkr.ecr.ap-southeast-1.amazonaws.com
#       # Alternative static approach (replace with your values)
#       # - docker login -u AWS -p $(aws ecr get-login-password --region us-east-1) 123456789012.dkr.ecr.us-east-1.amazonaws.com
  
#   build:
#     commands:
#       - echo Build started on `date`
#       - echo Building the Docker image...
#       # Build with explicit values
#       - docker build -t nginx-demo:latest .
#       - docker tag nginx-demo:latest 123456789012.dkr.ecr.us-east-1.amazonaws.com/nginx-demo:latest
  
#   post_build:
#     commands:
#       - echo Build completed on `date`
#       - echo Pushing the Docker image...
#       - docker push 123456789012.dkr.ecr.us-east-1.amazonaws.com/nginx-demo:latest